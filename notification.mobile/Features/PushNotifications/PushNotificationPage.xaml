<?xml version="1.0" encoding="utf-8"?>

<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pushNotifications="clr-namespace:notification.mobile.Features.PushNotifications;assembly=notification.mobile"
    xmlns:dtos="clr-namespace:notification.mobile.core.Dtos;assembly=notification.mobile.core"
    x:DataType="pushNotifications:PushNotificationViewModel"
    Shell.NavBarIsVisible="False"
    x:Class="notification.mobile.Features.PushNotifications.PushNotificationPage">

    <CollectionView
        ItemsSource="{Binding PushNotifications}">
        <CollectionView.EmptyView>
            <Label
                Text="No push notification yet..."/>
        </CollectionView.EmptyView>
        <CollectionView.ItemTemplate>
            <DataTemplate
                x:DataType="dtos:PushNotificationDto">
                <ContentView>
                    <StackLayout
                        Orientation="Vertical">
                        <Label
                            Text="{Binding Title}"/>
                        <Label
                            Text="{Binding Message}"/>
                    </StackLayout>
                </ContentView>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>